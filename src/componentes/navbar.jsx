import React from 'react';
import './navbar.css';

function Navbar() {
    return (
        <nav className="navbar">
            <a href="#">Productos</a>
            <a href="#">Nosotros</a>
            <a href="#">Contacto</a>
        </nav>
    );
}

export default Navbar;